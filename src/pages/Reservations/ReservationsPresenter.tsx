import React from 'react';
import styled from 'styled-components';

export const ReservationsPresenter: React.FC = () => {
  //TODO: Query - my reservations

  return <SContainer>Reservations</SContainer>;
};

const SContainer = styled.div`
  flex: 1;
`;
