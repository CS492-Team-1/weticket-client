import React from 'react';
import styled from 'styled-components';

export const LoginPresenter: React.FC = () => {
  return <SContainer>Login</SContainer>;
};

const SContainer = styled.div`
  flex: 1;
`;
