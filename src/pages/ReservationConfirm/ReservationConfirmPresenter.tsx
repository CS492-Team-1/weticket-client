import React from 'react';
import styled from 'styled-components';

export const ReservationConfirmPresenter: React.FC = () => {
  return <SContainer>ReservationConfirm</SContainer>;
};

const SContainer = styled.div`
  flex: 1;
`;
